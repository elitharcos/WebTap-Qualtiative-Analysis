VANTA.NET({
    el: "#vantaBackground",
    mouseControls: false,
    touchControls: false,
    gyroControls: false,
    minHeight: 200,        // Increase the minimum height
    minWidth: 200,         // Increase the minimum width
    scale: 0.8,            // Reduce the scale factor
    scaleMobile: 0.8,      // Reduce the scale factor for mobile devices
    color: 0x3f99ff,
    backgroundColor: 0xa0511,
    points: 10,            // Decrease the number of points
    maxDistance: 15,       // Slightly reduce the maximum distance between points
    spacing: 15,           // Slightly reduce the spacing between points
    showDots: false
});
